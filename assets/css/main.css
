/* style.css */

/* ================================== */
/*  تعريف الأنماط الأساسية وشاشة الموقع */
/* ================================== */

/* القاعدة العامة لتوحيد الفراغات والخطوط */
:root {
    --accent-color: #F5C518;
    --accent-hover-color: #d0a413;
    --accent-soft-color: #f9e88f;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Segoe UI', Tahoma, sans-serif;
}

/* خلفية الجسم ولون النص */
body {
    background-color: #2F2E33;
    color: white;
    margin: 0;
    padding: 0;
}



/* الهيكل العام للرأس */
header {
    background-color: #26262c;
    height: 65px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 20px;
    border-bottom: 1px solid #222;
    position: sticky;
    top: 0;
    z-index: 1000;
}

/* القسم الأيمن: القائمة + الشعار + اسم الموقع */
.header-right {
    display: flex;
    align-items: center;
    gap: 15px;
}

.menu-icon {
    color: var(--accent-color);
    font-size: 22px;
    cursor: pointer;
    padding: 5px;
    transition: color 0.3s ease;
}
.menu-icon:hover {
    color: var(--accent-hover-color);
}

.logo-box {
    display: flex;
    align-items: center;
    text-decoration: none;
    gap: 10px;
}

.logo-box img {
    height: 38px;
    width: auto;
    border-radius: 4px;
}

.site-name {
    color: white;
    font-size: 22px;
    font-weight: 800;
    letter-spacing: 0.5px;
    text-transform: capitalize;
}

.site-name span {
    color: var(--accent-color);
}

/* القسم الأيسر: البحث + زر الدخول */
.header-left {
    display: flex;
    align-items: center;
    gap: 20px;
}

.search-trigger {
    color: var(--accent-color);
    font-size: 18px;
    cursor: pointer;
    transition: color 0.3s ease;
}

.search-trigger:hover {
    color: var(--accent-hover-color);
}

.login-button {
    background-color: var(--accent-color);
    color: #000;
    text-decoration: none;
    font-weight: 500;
    font-size: 10px;
    height: 22px;
    padding: 0 9px;
    line-height: 22px;
    border-radius: 4px;
    white-space: nowrap;

    display: inline-flex;
    align-items: center;
    justify-content: center;

    transition: background-color 0.2s ease, transform 0.2s ease;
}

.login-button:hover {
    background-color: var(--accent-hover-color);
    transform: translateY(-2px);
}

/* مساحة تجريبية للمحتوى */
.main-content {
    padding: 40px 20px;
    text-align: center;
}

/* ========================= */
/* قسم البطل - Hero Style */
/* ========================= */
.hero-container {
    position: relative;
    width: 100%;
    height: 40vh;
    min-height: 280px;
    max-height: 460px;
    overflow: hidden;
    background: #000;
    display: flex;
    align-items: flex-end;
    padding: 0 5% 36px;
}

.hero-bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 38%;
    pointer-events: none;
    z-index: 1;
}

.hero-overlay {
    position: absolute;
    inset: 0;
    z-index: 2;
    /* التعديل: يبدأ من شفاف تماماً (0) وينتهي بظل أسود في الأسفل فقط (0.8) */
    background: linear-gradient(to top, rgba(0,0,0,0.8) 0%, rgba(0,0,0,0) 40%);
    pointer-events: none;
}

.hero-content {
    position: relative;
    z-index: 3;
    width: 100%;

    display: flex;
    justify-content: flex-start;
    gap: 12px;

    padding-bottom: 16px;
    box-sizing: border-box;

    user-select: none;
}

@media (min-width: 768px) {
    .hero-content {
        gap: 16px;
        padding-bottom: 24px;
    }
}


/* --- أزرار البطل الموحدة (تصميم مصغر ورشيق) --- */
.hero-buttons {
    display: flex;
    align-items: center;
    gap: 10px;       /* تقليل المسافة بين الأزرار */
    margin-top: 15px; /* تقليل الهامش العلوي */
    z-index: 5;
}

/* التنسيق الأساسي لتوحيد الحجم (Desktop) */
.btn-play, .btn-info {
    height: 36px;           /* تقليل الارتفاع من 48px إلى 36px */
    padding: 0 16px;        /* تقليل المساحة الجانبية */
    border-radius: 4px;     /* حواف أقل استدارة لتبدو أصغر بصرياً */
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: 13px;        /* تصغير الخط من 15px إلى 13px */
    font-weight: 600;       /* وزن متوسط للخط */
    text-decoration: none;
    transition: all 0.3s ease;
    border: none;
    cursor: pointer;
    white-space: nowrap;
}

/* زر التشغيل - الأصفر */
.btn-play {
    background-color: #ffc107;
    color: #000;
}

/* زر التفاصيل - الشفاف */
.btn-info {
    background: rgba(255, 255, 255, 0.12); /* شفافية أقل */
    color: #fff;
    backdrop-filter: blur(8px);
}

/* تأثيرات الحركة */
.btn-play:hover, .btn-info:hover {
    transform: translateY(-2px); /* حركة للأعلى بدلاً من التكبير */
    opacity: 0.9;
}

/* الأيقونات داخل الأزرار */
.btn-play i, .btn-info i {
    margin-left: 6px; 
    font-size: 12px; /* تصغير حجم الأيقونة */
}

/* نقاط التنقل (Dots) */
.hero-dots {
    position: absolute;
    left: 20px;
    top: 50%;
    transform: translateY(-50%);
    display: flex;
    flex-direction: column;
    gap: 12px;
    z-index: 3;
}

.hero-dot {
    width: 7px; height: 7px; /* تصغير حجم النقاط */
    background: rgba(255, 255, 255, 0.3);
    border-radius: 50%;
    cursor: pointer;
    transition: 0.3s;
}

.hero-dot.active {
    background: #ffc107;
    box-shadow: 0 0 8px #ffc107;
    transform: scale(1.2);
}

/* --- استجابة الموبايل للأزرار --- */
@media (max-width: 768px) {
    .hero-buttons {
        justify-content: center;
        margin-top: 10px;
    }
    .btn-play, .btn-info {
        height: 32px;    /* أصغر حجم ممكن للموبايل */
        padding: 0 12px;
        font-size: 12px;
    }
    .hero-dots { display: none; }
    
    .hero-container {
        height: 42vh;
        min-height: 280px;
        padding-bottom: 40px;
        text-align: center;
        justify-content: center;
        padding: 0 20px;
    }
    .hero-content {
        text-align: center;
    }
    .hero-overlay {
        background: linear-gradient(to top, rgba(0,0,0,0.8) 0%, rgba(0,0,0,0) 60%);
    }
}

/* ========================= */
/* القائمة الجانبية (Sidebar) */
/* ========================= */
.ani-overlay {
    position: fixed;
    top: 0; left: 0;
    width: 100%; height: 100%;
    background: rgba(47, 46, 51, 0.75);
    z-index: 9998;
    display: none;
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    transition: background 0.3s ease;
}

.ani-sidebar {
    position: fixed;
    top: 0;
    right: -300px;
    width: 280px;
    height: 100vh;
    background: rgba(47, 46, 51, 0.85);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    z-index: 9999;
    transition: right 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    display: flex;
    flex-direction: column;
    direction: rtl;
    border-left: 1px solid rgba(255, 255, 255, 0.06);
    box-shadow: -3px 0 10px rgba(0,0,0,0.6);
}

.ani-sidebar.active {
    right: 0;
}

.ani-close-wrapper {
    padding: 20px 15px;
}
.ani-close-btn {
    background: rgba(255, 255, 255, 0.1);
    color: #eee;
    border: 1px solid rgba(255, 255, 255, 0.12);
    padding: 8px 18px;
    border-radius: 20px;
    cursor: pointer;
    font-size: 14px;
    display: flex;
    align-items: center;
    gap: 10px;
    transition: background-color 0.3s ease, color 0.3s ease;
}
.ani-close-btn:hover {
    background: var(--accent-color);
    color: #2F2E33;
}

.ani-quick-actions {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    padding: 15px 0;
    background: rgba(0, 0, 0, 0.15);
    margin-bottom: 10px;
    border-radius: 5px;
    box-shadow: inset 0 0 10px rgba(245, 197, 24, 0.1);
}
.ani-action-item {
    text-align: center;
    color: #ccc;
    text-decoration: none;
    font-size: 11px;
    transition: color 0.3s ease;
}
.ani-action-item i {
    display: block;
    font-size: 20px;
    color: var(--accent-color);
    margin-bottom: 5px;
    filter: drop-shadow(0 0 1px #F5C518bb);
}
.ani-action-item:hover {
    color: var(--accent-color);
}

.ani-community-link {
    background: var(--accent-color);
    color: #2F2E33;
    text-decoration: none;
    margin: 10px 15px;
    padding: 12px;
    border-radius: 5px;
    text-align: center;
    font-size: 15px;
    font-weight: bold;
    display: block;
    transition: filter 0.3s ease;
}
.ani-community-link:hover {
    filter: brightness(1.15);
}

.ani-nav-list {
    list-style: none;
    padding: 0;
    margin: 0;
    overflow-y: auto;
    flex-grow: 1;
}
.ani-nav-list li {
    border-bottom: 1px solid rgba(255, 255, 255, 0.06);
}
.ani-nav-list a {
    color: #eee;
    text-decoration: none;
    display: block;
    padding: 15px 25px;
    font-size: 15px;
    transition: background-color 0.2s ease, color 0.2s ease;
}
.ani-nav-list a:hover {
    background: rgba(245, 197, 24, 0.15);
    color: var(--accent-color);
    box-shadow: inset 4px 0 var(--accent-color);
}
