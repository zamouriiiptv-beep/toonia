:root {
  --accent-color: #F5C518;
  --accent-hover-color: #d0a413;
  --accent-soft-color: #f9e88f;
}

/* ===== Reset ===== */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: 'Segoe UI', Tahoma, sans-serif;
}

body {
  background-color: #2F2E33;
  color: #fff;
  min-height: 100vh;
}

/* ===== Header ===== */
header {
  background-color: #26262c;
  height: 65px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0 20px;
  position: sticky;
  top: 0;
  z-index: 1000;
}

.header-right,
.header-left {
  display: flex;
  align-items: center;
  gap: 15px;
}

.menu-icon {
  color: var(--accent-color);
  font-size: 22px;
  cursor: pointer;
}

.logo-box {
  display: flex;
  align-items: center;
  gap: 10px;
  text-decoration: none;
}

.logo-box img {
  height: 38px;
}

.site-name {
  font-size: 22px;
  font-weight: 800;
  color: #fff;
}

.site-name span {
  color: var(--accent-color);
}

.login-button {
  background: var(--accent-color);
  color: #000;
  padding: 6px 12px;
  border-radius: 4px;
  font-size: 12px;
  text-decoration: none;
}

/* =========================
   HERO (مصحَّح للموبايل)
========================= */
.hero-container {
  position: relative;
  width: 100%;
  height: 40vh;
  min-height: 260px;
  max-height: 420px;
  overflow: hidden;
  display: flex;
  align-items: flex-end;
  padding: 0 5% 32px;
}

.hero-bg {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  z-index: 1;
}

.hero-overlay {
  position: absolute;
  inset: 0;
  z-index: 2;
  background: linear-gradient(
    to top,
    rgba(0,0,0,0.75),
    rgba(0,0,0,0.4),
    transparent
  );
}

.hero-content {
  position: relative;
  z-index: 3;
}

.hero-buttons {
  display: flex;
  gap: 14px;
}

.btn-play {
  background: var(--accent-color);
  color: #000;
  border: none;
  padding: 14px 35px;
  border-radius: 50px;
  font-weight: 800;
  cursor: pointer;
}

.btn-info {
  background: rgba(255,255,255,0.15);
  color: #fff;
  border: 1px solid rgba(255,255,255,0.3);
  padding: 14px 35px;
  border-radius: 50px;
  cursor: pointer;
}

/* ===== Hero Mobile Fix ===== */
@media (max-width: 768px) {
  .hero-container {
    height: 38vh;
    min-height: 240px;
    max-height: 340px;
    padding-bottom: 24px;
  }
}

/* ===== Overlay ===== */
.ani-overlay {
  position: fixed;
  inset: 0;
  background: rgba(47,46,51,0.75);
  opacity: 0;
  pointer-events: none;
  transition: opacity .3s ease;
  z-index: 9998;
}

.ani-overlay.show {
  opacity: 1;
  pointer-events: auto;
}

/* ===== Sidebar ===== */
.ani-sidebar {
  position: fixed;
  top: 0;
  right: -300px;
  width: 280px;
  height: 100vh;
  background: rgba(47,46,51,0.9);
  transition: right .4s ease;
  z-index: 9999;
  display: flex;
  flex-direction: column;
}

.ani-sidebar.active {
  right: 0;
}

.ani-close-wrapper {
  padding: 20px;
}

.ani-close-btn {
  width: 100%;
  padding: 10px;
  border-radius: 20px;
  border: none;
  cursor: pointer;
}

.ani-nav-list {
  list-style: none;
}

.ani-nav-list a {
  display: block;
  padding: 15px 25px;
  color: #fff;
  text-decoration: none;
}
